**0622 面向对象有哪些特征，并用你熟悉的语言描述如何实现。**

多态 polymorphism，封装 encapsulation，继承 Inheritance

封装是指类只暴露部分函数和成员变量供外界调用，其它内部实现对外是封闭不可见的。并且这种封装是有层级的，c++中可以通过public，protected，private修饰符来实现

继承是指子类默认具有父类已经实现的大部分函数，变量或者接口。

多态指子类可以对父类的函数进行重写和重载，重写和覆盖的不同是形参不同，覆盖是返回值、函数名和形参都完全一致。c++种父类的虚函数可以被子类用override关键字重写。